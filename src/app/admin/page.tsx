// src/app/admin/page.tsx
//
// PLACEHOLDER — the real reporting dashboard (filters by مدرسه/پایه/روز/ماه,
// full order/payment history) is built in Step 9. This exists only so the
// "/admin" nav link doesn't 404 in the meantime.

export default function AdminHomePage() {
    return (
        <p className="text-sm text-muted-foreground">
            گزارش‌ها در مرحله بعد اضافه می‌شود.
        </p>
    );
}